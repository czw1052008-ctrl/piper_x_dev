# DINOv3 ViT-S/16 backbone (local snapshot)

Copied from ModelScope `facebook/dinov3-vits16-pretrain-lvd1689m`.
Used frozen by `scripts/train_dinov3_seg.py`; only the 1×1 segmentation head is trained.

Files: `config.json`, `preprocessor_config.json`, `model.safetensors` (~83 MB).
